# 幕の内弁当コンポーザー 設計書

## 1. システム概要

### 1.1 目的
ユーザーがAI画像生成で作った料理画像を保管し、幕の内弁当の仕切りごとに
ドラッグ&ドロップで盛り付けて、オリジナルのお弁当を組み立てられるようにする。

### 1.2 ワークフロー
```
[ユーザー]
  ↓ プロンプト入力（例: 焼き鮭の切り身）
[画像生成エンジン]
  ↓ 料理画像（PNG）を生成し保管
[ライブラリ（保管庫）]
  ↓ 画像をドラッグ
[弁当の仕切り]
  ↓ ドロップして盛り付け
[ユーザー]
  ↓ 「献立を保存」で配置を永続化
```

---

## 2. 画像生成

### 2.1 現状（モック実装）
- 実APIは未接続。`backend/services/image_generator.py` の `generate_image(prompt, style)` が
  Pillow でプレースホルダー画像を生成する。
- プロンプト文字列のハッシュから配色を決定し、グラデーション背景＋お皿風の円＋
  プロンプト文字を描画する。
- 日本語描画のため IPAゴシック等の日本語フォントを優先的に読み込み、無ければ
  DejaVu（英数字）にフォールバックする。

### 2.2 実APIへの差し替え
`generate_image()` の **インターフェース（引数・戻り値=PNGバイト列）を維持**したまま
中身を置き換えるだけでよい。

| 候補 | 必要な環境変数 |
|------|----------------|
| OpenAI (gpt-image-1 / DALL·E) | `OPENAI_API_KEY` |
| Stability AI | `STABILITY_API_KEY` |

呼び出し元（`routers/images.py`）や保管処理（`storage.py`）は変更不要。

---

## 3. ストレージ

ファイルベースのシンプルな永続化（`backend/services/storage.py`）。

```
backend/storage/
├── images/<id>.png    ← 生成画像の本体
├── images.json        ← 画像メタデータ（id, prompt, style, url, created_at）
└── bento.json         ← 弁当レイアウト（仕切りID -> 画像ID）
```

- `storage/` は `.gitignore` 済み（生成物のためコミットしない）。
- 画像削除時は、その画像を使っている弁当の仕切りからも自動で除去する。
- 単一プロセス前提。並行アクセスは `threading.Lock` で直列化。

---

## 4. 弁当レイアウト（仕切り）

### 4.1 仕切り定義
`frontend/src/bento.ts` で6つの仕切りを定義する。

| ID | 役割 | 例 |
|----|------|----|
| rice | ご飯 | 白米・炊き込みご飯 |
| main | 主菜 | 焼き魚・唐揚げ |
| side1 | 副菜1 | 卵焼き・煮物 |
| side2 | 副菜2 | 和え物・揚げ物 |
| side3 | 副菜3 | サラダ・酢の物 |
| pickles | 香の物 | 漬物・梅干し |

### 4.2 レイアウト
CSS Grid の `grid-template-areas` で幕の内弁当らしい配置を表現する。

```
"rice main   main"
"rice side1  side2"
"rice side3  pickles"
```

左に縦長のご飯、右上に大きな主菜、右下に副菜・香の物。

---

## 5. ドラッグ&ドロップ

ネイティブの HTML5 Drag and Drop API を使用（追加ライブラリ不要）。

- 画像IDは `dataTransfer` の独自MIME `application/x-bento-image-id` で受け渡す
  （`frontend/src/dnd.ts`）。
- **ライブラリ → 仕切り**: 画像IDをコピーして仕切りへ配置。
- **仕切り → 仕切り**: 元の仕切りIDを `text/x-from-compartment` に載せ、
  ドロップ先で入れ替え（元の仕切りには移動先にあった画像が入る）。
- 仕切りの `×` ボタンでその仕切りを空にする。
- ドラッグ中の仕切りはハイライト表示する。

---

## 6. API設計

### 6.1 画像生成
```
POST /api/images/generate
Body: { "prompt": "焼き鮭の切り身", "style": "写実的" }
Response: { "id", "prompt", "style", "url", "created_at" }
```

### 6.2 画像一覧 / 削除
```
GET    /api/images          → { "images": [ ... ] }   # 新しい順
DELETE /api/images/{id}     → { "deleted": "<id>" }
```

### 6.3 弁当レイアウト
```
GET /api/bento              → { "compartments": { "rice": "<id>" }, "updated_at" }
PUT /api/bento
Body: { "compartments": { "rice": "<id>", "main": null } }
（値が null の仕切りは保存しない）
```

### 6.4 画像配信
```
GET /media/images/{id}.png  → image/png
```

---

## 7. フロントエンド画面構成

```
┌───────────────────────────────────────────────────────────┐
│  🍱 幕の内弁当コンポーザー                                  │
├──────────────────────────┬────────────────────────────────┤
│ ① 料理画像を生成          │ ③ お弁当を盛り付け             │
│  [プロンプト入力]         │  ┌──────┬─────────────┐        │
│  [スタイル: 指定なし…]    │  │      │   主菜       │        │
│  [ 画像を生成 ]           │  │ ご飯 ├──────┬──────┤        │
│                          │  │      │副菜1 │副菜2 │        │
│ ② 画像ライブラリ（保管庫）│  │      ├──────┴──────┤        │
│  [🖼][🖼][🖼][🖼]         │  │      │  香の物      │        │
│   ↑ ドラッグして仕切りへ  │  └──────┴─────────────┘        │
└──────────────────────────┴────────────────────────────────┘
```

---

## 8. テスト

`backend/tests/test_images.py`（pytest + FastAPI TestClient）

- 画像の生成・一覧・削除・配信
- 空プロンプトの拒否
- 弁当レイアウトの保存・取得
- 画像削除時に弁当の仕切りからも除去されること

ストレージは `tmp_path` に差し替えてテストする。

---

## 9. 今後の拡張

| # | 項目 | 内容 |
|---|------|------|
| 1 | 実画像生成API接続 | OpenAI / Stability AI 等へ差し替え |
| 2 | 複数弁当の保存 | 現状は単一レイアウト。複数献立の管理 |
| 3 | 完成弁当の書き出し | 盛り付け結果を1枚の画像としてダウンロード |
| 4 | 仕切りレイアウトの選択 | 幕の内以外の弁当箱（松花堂等）に対応 |
| 5 | 認証・マルチユーザー | ユーザーごとのライブラリ・献立 |
